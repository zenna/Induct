// Sample from posterior distribution over actions and rewards
// Solve for paticular T 
var thompsonSample = function() {
	return action;
}

var GrowSparseBayesianTree = function(node, budget, p, horizon) {
	
}

var BayesDescend = function(node,p) {
	if (node['type'] = 'decision') {
		var alpha = thompsonSample(node['beliefState']);
		if (!alpha in node['children'])
	}
}